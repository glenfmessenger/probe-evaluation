__pycache__/
*.pyc
.DS_Store
*.egg-info/
.pytest_cache/
# never commit model weights or HF caches
*.safetensors
*.bin
*.gguf
*.pt
*.pth
hf_cache/
.venv/
logs_env_install.log
# Activation tensors only. Per-case scores are committed -- see the note in gates_config.yaml, and the guard
# in tests/test_results_completeness.py that fails if an aggregate AUC ships without them.
results/phase2/activations/
results/gates/activations/
# LaTeX build artifacts
paper/*.pdf
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
# IEEE template files. NOT redistributable, so they are deliberately not committed: obtain ieeeaccess.cls and
# IEEEtran.bst from the IEEE Access author portal (the "LaTeX template" download) and place both in paper/.
# `make check` fails with these instructions until they are present, so that error message stays the onboarding path
# for anyone cloning the repository.
paper/ieeeaccess.cls
paper/IEEEtran.bst
# ieeeaccess.cls \includegraphics these; they ship in the same template download and are equally
# not ours to redistribute.
paper/Logo.png
paper/notaglineLogo.png
paper/bullet.png
paper/build-smoke/
paper/aase-successor-PREVIEW.pdf
